import { env } from '$env/dynamic/public'

export const {
  PUBLIC_PB_DOMAIN,
  PUBLIC_PB_SUBDOMAIN,
  PUBLIC_APP_DOMAIN,
  PUBLIC_APP_PROTOCOL,
  PUBLIC_PB_PROTOCOL,
  PUBLIC_POCKETHOST_VERSION,
} = env
