{
  "name": "pockethost-mothership-app",
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "check:fresh": "tsdown && git diff --exit-code -- pb_hooks/"
  },
  "devDependencies": {
    "tsdown": "^0.22.2",
    "type-fest": "^5.7.0"
  }
}
