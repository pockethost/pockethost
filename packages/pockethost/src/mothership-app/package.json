{
  "name": "pockethost-mothership-app",
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "check:fresh": "tsdown && git diff --exit-code -- pb_hooks/"
  },
  "devDependencies": {
    "tsdown": "^0.12.9",
    "type-fest": "^4.6.0"
  }
}
